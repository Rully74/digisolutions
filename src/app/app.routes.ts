import { Routes } from '@angular/router';
import { MotivoListComponent } from './features/motivo-list/motivo-list.component';

export const routes: Routes = [
    {
        path: 'list',
        component: MotivoListComponent
    }
];
